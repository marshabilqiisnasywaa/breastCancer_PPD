services:
  - type: web
    name: breastcancer-ppd-api
    env: python
    region: singapore
    plan: free
    buildCommand: "cd Api && pip install -r requirements.txt"
    startCommand: "cd Api && gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
